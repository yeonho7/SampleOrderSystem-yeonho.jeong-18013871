# CLAUDE.md — 반도체 시료 생산주문관리 시스템

## 프로젝트 개요

가상의 반도체 회사 S-Semi를 위한 **콘솔 기반 CLI 애플리케이션**.
시료 등록, 주문 접수·승인·거절, 생산라인 관리, 출고 처리를 통합한다.
요구사항 전체는 `docs/PRD.md`를 참고한다.

- **언어**: Python 3
- **실행**: `python main.py`
- **가상환경**: `.venv` (`.venv\Scripts\python.exe`)

---

## 아키텍처

MVC 패턴을 따른다. 레이어 간 의존 방향은 단방향이다.

```
View → Controller → Model
```

---

## 핵심 도메인 규칙

### 주문 상태 전이

```
RESERVED → (승인 + 재고 충분) → CONFIRMED → RELEASE
RESERVED → (승인 + 재고 부족) → PRODUCING → CONFIRMED → RELEASE
RESERVED → (거절)             → REJECTED
```

유효하지 않은 상태 전이는 예외로 처리한다.

### 재고 상태 판단 (모니터링)

| 조건 | 표시 |
|------|------|
| `stock == 0` | 고갈 |
| `stock < 미처리 주문 수량 합계` | 부족 |
| 그 외 | 여유 |

---

## 데이터 영속성

- JSON 파일 방식을 사용한다 (`data/*.json`).
- Repository 계층이 직렬화/역직렬화를 전담한다.
- Controller는 Repository 인터페이스만 호출한다.
- 애플리케이션 시작 시 `data/` 파일을 로드하고, 변경 시마다 즉시 저장한다.

---

## 코딩 규칙

- Model 클래스는 순수 데이터 객체로 유지한다 (비즈니스 로직 금지).
- 비즈니스 로직은 Controller에만 작성한다.
- View는 출력 전용이다 — 계산이나 상태 변경을 View에 두지 않는다.
- `OrderStatus`는 `Enum`으로 정의하고, 문자열 비교를 하지 않는다.
- 주문번호 형식: `ORD-YYYYMMDD-NNNN` (날짜 기반 순번).

---

## 주의 사항

- `REJECTED` 주문은 모니터링 집계에서 항상 제외한다.
- 출고(`RELEASE`) 처리는 `CONFIRMED` 상태에서만 가능하다.
- 생산라인 큐는 **FIFO** 순서를 보장해야 한다.
- 등록되지 않은 시료 ID로 주문 시 즉시 에러 메시지를 출력하고 취소한다.

## 커밋 정책

작업 단위가 완결될 때마다 즉시 커밋한다. 커밋은 최대한 자주, 작은 단위로 한다.
- 테스트 통과 → 커밋
- 기능 구현 → 커밋
- 버그 수정 → 커밋

커밋을 모아두지 않는다. 진행 상황을 항상 git 히스토리로 추적할 수 있어야 한다.